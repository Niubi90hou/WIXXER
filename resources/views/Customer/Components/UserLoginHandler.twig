<script type="x/template" id="vue-user-login-handler">
<div id="login-change">
<div v-if="!isLoggedIn">
<ul class="ht-dropdown">
    <li><a data-toggle="modal" href="{% if services.template.isCurrentTemplate("tpl.login") %}javascript:void(0){% else %}#login{% endif %}" @click="unmarkInputFields()">{{ trans("Ceres::Template.login") }}</a></li>
    <li><a data-toggle="modal" href="#registration" @click="unmarkInputFields()" v-if="showRegistration">{{ trans("Ceres::Template.loginRegister") }}</a></li>
</ul>
</div>


<div v-if="isLoggedIn">
<ul class="ht-dropdown">
    <li><a href="{{ urls.myAccount }}">{{ trans("Ceres::Template.loginMyAccount") }}</a></li>
    <li><a href="#" v-logout>{{ trans("Ceres::Template.loginLogout") }}</a></li>

</ul>
</div>

</div>

</script>
